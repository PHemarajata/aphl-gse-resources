{
  "name": "aphl-gse-resources",
  "private": true,
  "description": "APHL genomic epidemiology resources — static site and curation tooling",
  "scripts": {
    "build": "node scripts/build-data.mjs && node scripts/build-schema.mjs",
    "check": "node scripts/build-schema.mjs --check",
    "validate": "npm run build && npm run check && node scripts/validate-resources.mjs",
    "preview": "npm run build && node scripts/preview.mjs",
    "new": "node scripts/new-resource.mjs",
    "test": "npm run validate"
  }
}
